import type {MspPowerConfigurationSnapshot} from '../decoding/decodePowerConfiguration'; import {createPowerConfigurationDraft} from '../../../state/powerConfigurationModel'; import {encodeChangedPowerConfiguration} from './encodePowerConfiguration';
const snapshot: MspPowerConfigurationSnapshot = {battery: {minCellCentivolts: 330, maxCellCentivolts: 430, warningCellCentivolts: 350, capacityMah: 1500, voltageMeterSource: 1, currentMeterSource: 1}, voltageMeters: [{id: 10, sensorType: 0, scale: 110, divider: 10, multiplier: 1}], currentMeters: [{id: 10, sensorType: 1, scale: 400, offset: 0}]};
describe('encode power configuration', () => {it('encodes exact battery and signed calibration payloads', () => {const draft = createPowerConfigurationDraft(snapshot); const writes = encodeChangedPowerConfiguration(snapshot, {...draft, capacityMah: 1800, currentMeters: [{...draft.currentMeters[0], scale: -1000}]}); expect([...writes[0].payload]).toEqual([33, 43, 35, 8, 7, 1, 1, 74, 1, 174, 1, 94, 1]); expect([...writes[1].payload]).toEqual([10, 24, 252, 0, 0]);});});
